angular.module('karmic-core', ['ui.router'])
    .config([function () {
        // karmic-core-specific config tasks go here.
        // add global routes to the karmic-core config/routes.js file
    }]);
